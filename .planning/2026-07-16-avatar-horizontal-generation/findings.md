# Findings

## 2026-07-16

- The existing user-supplied Goal Prompt is complete enough to serve as the approved design; no additional creative clarification is required.

## 2026-07-17 independent acceptance review

- Fresh verification passed: 19/19 horizontal-validator tests, Python compilation, and strict validator exit code 0.
- The strict validator independently found 28/28 targets, 28/28 automated technical passes, 81 baseline entries unchanged, zero non-target changes, a hash-matched 28-entry manual manifest, and overall status `pass`.
- Independent inspection of the 7x4 transparent sheet found all 28 cells populated with the correct actor, strict left/right facing, empty-handed Walk state, and one-folder Carry state. No visible background, shadow, text, extra prop, direction reversal, or obvious mirrored clone was found.
- Independent inspection of the 7x8 movement comparison found stable identity and palette against each actor's up/down references. Horizontal Carry folders are intentionally more prominent than the up/down folders, most noticeably for Bob and Quinn; office-shell review is required before accepting that emphasis at scene scale.
- Independent inspection of the 28-cell office-shell composite passes: all actors read at a coherent scene scale against tiles and door frames, feet remain grounded, strict horizontal direction remains legible, and the larger horizontal folders still read as handoff objects without obscuring actor identity.
- The validation JSON stores asset records at `targets.assets`, not a top-level `assets` array. One exploratory PowerShell read used the wrong field, failed after printing the valid summary, and was corrected without modifying any asset.
- Independent metric cross-check confirms 28 unique SHA-256 hashes, zero duplicate files, and 109 total project PNGs. Left/right runtime heights remain close; maximum source-foot Y delta is 41px for Quinn Carry (about 5.9px at 180px), which is acceptable only if integration consumes each image's stored foot anchor rather than a shared CSS offset.
- Original-resolution inspection of Quinn `carry-left` and `carry-right` passes. Hair, oversized square glasses, folder/hand contact, and anatomical-left chest-badge visibility are physically consistent; the pair is independently generated and free of residue or detached fragments.
- Original-resolution inspection of the highest-retry final files also passes: Kara `carry-right` has no unauthorized earring and keeps the chest badge in the correct location; Rita `carry-right` has a continuous ponytail/body component, correct white blouse identity, and clean two-hand folder contact.
- The current workspace and its dirty state are part of the accepted 81-image baseline. A separate clean worktree would omit those uncommitted baseline assets, so this task must operate in the current workspace while restricting writes to new horizontal assets, temporary candidates, and audit artifacts.
- Three read-only subagents are checking reference files, reusable validation tooling, and the prior ImageGen save/normalization workflow.
- Preflight found exactly 81 existing PNG files and all seven actor folders contain the expected ten current avatar assets. None of the 28 horizontal target filenames exists.
- Two SHA-256 scans 1.5 seconds apart were identical, so no concurrent `images/` writer was observed during baseline capture.
- The immutable baseline is recorded in `baseline-sha256.csv` with 81 rows.
- Independent reference audit confirmed 49/49 required actor reference images exist, decode correctly, and are uniformly PNG/RGBA/1254×1254 with alpha; all 28 target names remain absent.
- Alice's viewed references consistently show the high round brown bun, dark long-sleeve outfit, compact office-map proportions, and a true side movement profile. Existing side references contain contact shadows, but the new assets must omit those shadows per the approved prompt.
- Existing movement alpha bboxes provide useful scale bounds. Alice ranges from roughly 303–659 px wide and 640–885 px high depending on state; new horizontal assets should stay comparable while remaining centered and padded.
- Bob references confirm fluffy rounded black hair, thick rectangular black glasses, a deep navy collared top with a pale vertical placket, black trousers, and a comparatively slim/tall body. New Bob side profiles must keep the glasses readable and omit any chest badge.
- Jack references confirm spiky asymmetric black hair, no glasses, saturated cobalt collared top, and black trousers/shoes. The side-view movement reference has one small white left-chest mark; the generated opposite direction must preserve physically correct occlusion instead of mirroring it.
- Kara references confirm saturated purple shoulder-length hair with a rounded crown and outward-curled ends, purple top, dark trousers, brown shoes, and a petite silhouette. Her single gold four-corner badge is visible only when the left chest is exposed; it must not migrate to a sleeve or duplicate.
- Leo references confirm black side-part hair with a swept forelock, rectangular glasses, muted dark-blue collared top, small pale square left-chest badge, and slim proportions. Badge visibility must follow the chosen side-profile occlusion.
- Quinn references confirm neat rounded black hair with a small rear tuft, oversized square black glasses, bright royal-blue top, white square left-chest badge, and a compact silhouette. The glasses occupy much of the side profile and must remain square rather than becoming generic spectacles.
- Rita's `idle.png` uses loose hair and must not control the movement hairstyle. Her walk/carry and up/down references consistently establish warm chestnut side bangs plus a mid-high arched ponytail, white collared top, narrow dark neckline detail, dark trousers, and black shoes; this movement identity is authoritative.
- Built-in ImageGen outputs are available under `C:/Users/29929/.codex/generated_images/<thread-id>/exec-*.png`; the project-bound source must be copied from there into the required `tmp/imagegen/horizontal-movement/...` path immediately after each call.
- `tmp/imagegen/validation/avatar_asset_validator.py` already provides alpha bbox, green-pixel, connected-component, stable foot-anchor, 180px render, contact-sheet, and office-shell composite primitives. Its default target constants are locked to the older up/down set, so the horizontal task needs a thin wrapper or import-time override rather than running its CLI unchanged.
- The installed `remove_chroma_key.py` accepts the exact approved soft-matte/despill options and supports the conditional `--edge-contract 1` retry.
- Historical evidence shows 72/72 square built-in avatar outputs were natively 1254×1254. Source copies preserved the generated SHA exactly; alpha candidates and promoted official files also preserved SHA exactly. Wrong-size generations must be rejected and regenerated, never resized.
- The existing validator can analyze arbitrary candidates directly. The horizontal wrapper must supply the exact 28-path whitelist and custom contact/composite layouts because the old CLI is hard-coded to up/down targets.
- The existing whole-repository audit is intentionally fixed to the current 81-file registry and will classify the 28 new horizontal files as unregistered. This task will therefore run a dedicated horizontal audit while preserving frontend/layout files unchanged; full registry integration is outside the approved scope.
- Alice `walk-left` attempt 1 is natively 1254×1254 and passed every automated technical check: RGBA, transparent corners, clear edges, one 8-connected component, and zero visible green/chroma-key pixels. Alpha bbox is `[410,214,813,972]`; stable foot anchor is `(579,970)`.
- Visual review of Alice `walk-left` attempt 1 passes: unmistakable high round brown bun, black outfit, strict left profile, natural mid-stride, empty hands, no shadow/outline/object, and no cropping. Accepted candidate: `tmp/imagegen/horizontal-movement/Alice/walk-left/attempt-1-alpha.png`.
- Alice `walk-right` attempt 1 passed all automated checks with bbox `[439,221,823,950]`, zero visible green/chroma pixels, one component, and stable foot anchor `(626.5,949)`.
- Visual review of Alice `walk-right` passes: strict right profile, empty hands, correct identity and black outfit, no shadow/object/crop. It is independently posed rather than a horizontal mirror of `walk-left` (different arm/stride phase and hair/occlusion structure). Walk pair height difference is about 4%, within the 10% group tolerance.
- Alice `carry-left` attempt 1 passed every automated check with bbox `[426,148,849,1035]`, zero visible green/chroma pixels, one component, and stable foot anchor `(627.5,1033)`.
- Visual review of Alice `carry-left` passes: strict left profile, correct high bun and black outfit, exactly one blank light-tan tabbed folder, clear two-hand contact, no extra paper/object/shadow/text, and generous padding.
- Alice `carry-right` attempt 2 passed every automated check with bbox `[410,145,856,1046]`, zero visible green/chroma pixels, one component, and stable foot anchor `(637,1045)`. Attempt 1 produced no image because of a network error.
- Visual review of Alice `carry-right` passes: strict right profile, one blank light-tan tabbed folder with clear two-hand contact, correct identity/outfit, and no extra object, shadow, text, or crop. Its stride, folder angle, and hair occlusion differ from the left asset, so it is not a mirrored copy.
- A new horizontal-specific validator is being built test-first in `tools/validate_horizontal_avatar_assets.py`; the exact 28-file whitelist, present/missing technical analysis, and transparent 7×4 contact-sheet contract already have observed red→green tests.
- Alice group QA at true 180×180 rendering and office-shell placement passed all four assets. Walk left/right source-bbox height difference is 4.0%; carry left/right difference is 1.6%. Both Carry folders remain distinct at scene scale.
- Alice was promoted as a four-file group only after 4/4 acceptance. The old 81-file baseline was reverified immediately beforehand with zero mismatches, and candidate→official SHA-256 matched for all four new files.
- Bob `walk-left` attempt 1 passed all automated checks with bbox `[399,177,839,1012]`, one component, zero visible green/chroma pixels, and foot anchor `(614.5,1011)`. Visual review passes the strict left profile, fluffy black hair, rectangular glasses, navy top with pale center placket, slim build, empty hands, and no badge/shadow/object/crop.
- Bob `walk-right` attempt 1 passed single-image technical/identity/direction checks but was rejected at group scale QA: bbox height 948px versus 835px for `walk-left` (13.5% larger). No resizing will be used; attempt 2 targets only an approximately 8% smaller native subject scale.
- Bob `walk-right` attempt 2 passed all technical and visual checks with bbox `[416,179,832,999]`, height 820px, foot anchor `(629,998)`, and zero visible green/chroma pixels. Its height differs from accepted `walk-left` by only 1.8%; strict right profile, empty hands, glasses, no badge, and independent stride/occlusion all pass.
- Bob `carry-left` attempt 1 passed all checks with bbox `[386,184,900,1035]`, one component, zero visible green/chroma pixels, and foot anchor `(655.5,1033)`. Visual review confirms strict left direction, Bob identity/no badge, exactly one blank tabbed folder with two-hand contact, and no extra object/shadow/text/crop.
- Bob `carry-right` attempt 1 passed all checks with bbox `[338,140,928,1051]`, one component, zero visible green/chroma pixels, and foot anchor `(587.5,1049)`. It is a strict independently posed right profile with Bob's glasses/navy placket/no badge and exactly one blank tabbed folder. Carry pair height difference is 7.1%, within tolerance.
- Bob group QA passed at actual 180×180 and in office-shell placement. Walk pair height difference is 1.8%; carry pair is 7.1%. All four preserve rectangular glasses, navy top/pale placket, slim scale, and no badge.
- Jack `walk-left` attempt 1 passed every automated check with bbox `[398,145,863,998]`, one connected component, zero visible green/chroma pixels, and stable foot anchor `(644,997)`. Original-resolution visual review confirms a strict left-facing side profile, asymmetric spiky black hair, no glasses, cobalt collared top, black trousers/shoes, empty hands, and no object/shadow/crop. The anatomical left-chest mark is correctly occluded on this far-side view. Accepted candidate: `tmp/imagegen/horizontal-movement/Jack/walk-left/attempt-1-alpha.png`.
- Jack `walk-right` attempt 1 passed every automated check with bbox `[420,154,844,1012]`, one component, zero visible green/chroma pixels, and stable foot anchor `(623.5,1011)`. Visual review confirms a strict right profile, empty hands, Jack's spiky black hair/no glasses/cobalt top, and exactly one small white mark on the visible anatomical left chest. The stride and occlusion are independently generated; walk-pair bbox heights differ by only 0.6%.
- Jack `carry-left` attempt 1 passed every automated check with bbox `[364,173,829,1032]`, one component, zero visible green/chroma pixels, and stable foot anchor `(627.5,1031)`. Visual review confirms a strict left profile, correct identity, the left-chest mark correctly occluded, and exactly one blank light-tan tabbed folder held by both hands; no extra paper/object/shadow/text/crop is present.
- Jack `carry-right` attempt 1 passed every automated check with bbox `[357,142,933,1024]`, one component, zero visible green/chroma pixels, and stable foot anchor `(597,1023)`. Visual review confirms a strict independently posed right profile, exactly one visible white left-chest mark, and one blank light-tan tabbed folder contacted by both hands. Carry-pair bbox heights differ by 2.7%, within tolerance.
- Jack group QA passed at exact 180×180 runtime scaling and in the office-shell crop. Walk pair height difference is 0.6%; carry pair is 2.7%. Direction, foot placement, spiky-hair/no-glasses identity, side-correct chest-mark visibility, and Carry-folder readability all pass.
- Jack was promoted 4/4 only after the original 81-file baseline reverified with zero mismatches. Candidate-to-official SHA-256 matched for all four files: `9c38d90e…` walk-left, `aa747615…` walk-right, `1626ccda…` carry-left, and `63a25701…` carry-right.
- Completion audit caught that the initial Jack Carry promotion mistakenly inserted a `Carry/` subfolder. This was corrected immediately: the two new files were copied without overwrite to the exact whitelist paths `images/avatars/Jack/carry-left.png` and `carry-right.png`, hashes reverified unchanged, the misplaced copies removed, and the now-empty erroneous directory removed. No baseline file was involved.
- Kara `walk-left` attempt 1 passed every automated check with bbox `[429,221,834,970]`, one component, zero visible green/chroma pixels, and stable foot anchor `(617.5,969)`. Original-resolution visual review confirms Kara's petite proportions, saturated purple shoulder-length hair with outward ends, purple top/dark trousers/brown shoes, a strict left profile, empty hands, and correctly occluded left-chest gold badge.
- Chroma-source audit shows ImageGen's successful green-background outputs use near-green border colors rather than byte-identical flat `#00ff00`; this is an upstream source-generation deviation. The required processed alpha candidates have transparent corners and zero visible green/chroma pixels. Final reporting must distinguish source chroma quality from the official transparent PNG acceptance state.
- Kara `walk-right` attempt 1 passed every automated check with bbox `[432,218,828,1000]`, one component, zero visible green/chroma pixels, and stable foot anchor `(637.5,999)`. Visual review confirms a strict right profile, empty hands, correct purple-hair/petite identity, and exactly one gold four-corner badge on the visible anatomical left chest. Walk-pair bbox heights differ by 4.4%.
- Kara `carry-left` attempt 1 passed every automated check with bbox `[408,217,831,960]`, one component, zero visible green/chroma pixels, and stable foot anchor `(628,959)`. Visual review confirms a strict left profile, petite purple-haired identity, correctly occluded badge, and exactly one blank light-tan tabbed folder contacted by both hands.
- Kara `carry-right` attempt 1 passed automated checks with bbox `[416,220,866,949]` but failed identity/accessory review: it added an unauthorized gold earring not present in Kara's references. The chest badge/folder/direction were otherwise correct, but any new accessory is disqualifying. Attempt 1 is retained as rejected evidence; attempt 2 will target removal of all ear jewelry and allow gold only in the single left-chest badge.
- Kara `carry-right` attempt 2 removed the unauthorized earring and passed single-image technical/identity/action checks, but failed the group-scale gate: bbox height 833px versus 743px for accepted `carry-left`, a 12.1% difference above the 10% limit. No resizing will be used. Attempt 3 will use the accepted left candidate solely as a native-scale reference and target an approximately 11% smaller subject.
- Kara `carry-right` attempt 3 passed all single-image checks with bbox `[410,173,878,999]`, zero visible green/chroma pixels, one component, stable foot anchor `(621.5,997)`, no earring, one correctly placed visible gold badge, and one blank folder held by both hands. Its 826px bbox height is slightly reduced but still 11.2% above the original 743px left candidate. Because this right asset has exhausted three attempts but is otherwise fully valid, group correction will use `carry-left` attempt 2 (only its second attempt) to natively target 780–810px; no asset will be resized.
- A reconnect interrupted the first `carry-left` attempt-2 call before it produced any ImageGen file; it is recorded as an interrupted/no-output call, not a candidate. The repeated calibration call produced `carry-left` attempt 2 with bbox `[388,135,862,1016]`, height 881px, one component, zero visible green/chroma pixels, and foot anchor `(643,1015)`. It passes strict left direction, no badge/no earring, and one blank folder with two-hand contact.
- Kara group QA passed at actual 180×180 and in office-shell placement using walk-left a1, walk-right a1, carry-left a2, and carry-right a3. Walk pair height difference is 4.4%; final Carry pair difference is 6.7%. All badge visibility, direction, folder, and petite purple-hair identity checks pass.
- Kara was promoted 4/4 only after a fresh 81-file baseline precheck returned zero mismatches and all four official SHA-256 values matched their accepted candidates.
- Leo `walk-left` attempt 1 passed every automated check with bbox `[437,221,821,970]`, one component, zero visible green/chroma pixels, and stable foot anchor `(635.5,969)`. Visual review confirms a strict left profile, rectangular glasses, black side-part/swept hair, muted blue-gray top, slim proportions, empty hands, and correctly occluded left-chest badge.
- Leo `walk-right` attempt 1 passed every automated check with bbox `[444,212,824,966]`, one component, zero visible green/chroma pixels, and stable foot anchor `(626.5,965)`. Visual review confirms strict right direction, empty hands, rectangular glasses, and exactly one pale square badge on the visible anatomical left chest. Walk-pair bbox heights differ by 0.7%.
- Leo `carry-left` attempt 1 passed every automated check with bbox `[384,177,866,1021]`, one component, zero visible green/chroma pixels, and foot anchor `(651,1020)`. It passes strict left direction, rectangular-glasses identity, badge occlusion, and exactly one blank tabbed folder held by both hands.
- Leo `carry-right` attempt 1 passed every automated check with bbox `[389,177,887,997]`, one component, zero visible green/chroma pixels, and foot anchor `(598,996)`. It passes strict right direction, one visible pale left-chest square badge, and one blank tabbed folder with two-hand contact. Carry-pair bbox heights differ by 2.9%.
- Leo group QA passed at exact 180×180 and in the office shell; all four assets were promoted after a zero-mismatch 81-file baseline precheck and candidate-to-official SHA equality.
- Quinn `walk-left` attempt 1 passed every automated check with bbox `[430,242,802,923]`, one component, zero visible green/chroma pixels, and foot anchor `(611.5,922)`. Visual review confirms strict left direction, compact build, rounded black hair with rear tuft, oversized square glasses, royal-blue top, empty hands, and badge occlusion.
- Quinn `walk-right` attempt 1 passed every automated check with bbox `[430,254,819,921]`, one component, zero visible green/chroma pixels, and foot anchor `(613.5,920)`. Visual review confirms strict right direction, matching compact identity, and exactly one white square badge on the visible anatomical left chest. Walk-pair bbox heights differ by 2.1%.
- Quinn `carry-left` attempt 1 passed every automated check with bbox `[359,167,885,1021]`, one component, zero visible green/chroma pixels, and foot anchor `(663.5,1020)`. It passes strict left direction, large-square-glasses/compact identity, badge occlusion, and one blank folder with two-hand contact.
- Quinn `carry-right` attempt 1 passed every automated check with bbox `[380,192,902,981]`, one component, zero visible green/chroma pixels, and foot anchor `(595.5,979)`. It passes strict right direction, one visible white left-chest square badge, and one blank folder held by both hands. Carry-pair bbox heights differ by 8.2%.
- Quinn group QA passed at exact 180×180 and in office-shell placement; all four were promoted after zero baseline mismatches and candidate-to-official SHA equality.
- Rita `walk-left` attempt 1 passed every automated check with bbox `[404,218,889,981]`, one component, zero visible green/chroma pixels, and foot anchor `(593.5,980)`. Visual review confirms strict left direction, warm chestnut side bangs and tied mid-high arched ponytail trailing right, white collared top/dark neckline detail, empty hands, and no idle loose hair/Alice bun.
- Rita `walk-right` attempt 1 passed every automated check with bbox `[355,215,854,987]`, one component, zero visible green/chroma pixels, and foot anchor `(633,986)`. Visual review confirms strict right direction, the movement-authoritative ponytail trailing left, correct white/dark outfit, and empty hands. Walk-pair bbox heights differ by 1.2%.
- Rita `carry-left` attempt 1 passed every automated check with bbox `[334,154,977,1035]`, one component, zero visible green/chroma pixels, and foot anchor `(626,1034)`. Visual review confirms strict left direction, movement-authoritative arched ponytail trailing right, white collared outfit, and exactly one blank folder contacted by both hands.
- Rita `carry-right` attempt 1 passed single-image technical/identity/direction/action checks with bbox `[348,199,894,991]`, but its 792px height is 11.2% below the accepted left candidate's 881px height, narrowly failing the 10% group-scale gate. It is retained as rejected evidence; attempt 2 will natively target 850–880px with no resizing.
- Rita `carry-right` attempt 2 overshot to bbox `[263,118,971,1109]` (991px high) and failed automated technical QA with two disconnected alpha components, caused by a detached ponytail fragment. It is rejected. Attempt 3 will use both prior right attempts as lower/upper scale bounds, target 850–900px, and require one fully connected subject/folder component.
- Rita `carry-right` attempt 3 passed every automated check with bbox `[275,161,922,1064]`, one component, zero visible green/chroma pixels, and foot anchor `(598.5,1062)`. Visual review confirms strict right direction, connected movement-authoritative ponytail trailing left, correct outfit, and exactly one blank folder held by both hands. Final Carry-pair bbox heights differ by 2.5%.
- Rita group QA passed at exact 180×180 and in the office shell. All four were promoted after a fresh 81-file baseline check returned zero mismatches and candidate-to-official SHA values matched. The exact 28-file horizontal whitelist is now populated.
- Bob was promoted 4/4 after a fresh 81-file baseline verification with zero changes; all four candidate→official SHA-256 comparisons matched.
- Final transparent contact-sheet review confirms 28/28 isolated sprites are present in the required 7x4 order. Every Walk is empty-handed; every Carry has exactly one blank light-tan folder; all left/right profiles point toward the filename direction and show independently generated stride/occlusion details.
- Final 7x8 movement comparison review confirms each actor's new horizontal set remains recognizable beside its walk-up/down and carry-up/down references, including direction-correct badge visibility and Rita's movement ponytail.
- Final 180px office-shell composite review confirms all 28 sprites remain readable and correctly scaled at the PM-to-Hub, Dev-to-Hub, and QA-to-Hub representative positions. Feet sit plausibly on the route plane, no asset is clipped, and Carry folders remain legible.
# 2026-07-17 前端接入现状审查

- 当前 `docs/office-layout.json` 每个角色仍只登记 10 个姿势：4 个旧姿势与 6 个背面/纵向新姿势；本轮新增的 `walk-left/right`、`carry-left/right` 尚未进入布局清单。
- 当前 `MovementDirection` 仍将水平方向合并为 `horizontal`，`resolveMovementPose` 因此继续返回旧的 `walk` / `carry` 资产，无法区分向左与向右。
- 当前坐姿仍由 `hasActiveWork` 决定 `seatedIdleBack` 或 `seatedWorkingBack`，与已确认的产品规则“员工上线即判断正在工作”不一致。
- 现有测试仍以 81 张登记图片、每名角色 10 个姿势及水平移动使用旧资产为基线。
- 因 28 张水平资产的脚底锚点存在客观差异，接入时必须登记并使用每张图片自己的脚底锚点；不能依赖统一 CSS 位移。
- 下一项开发任务应为“四方向 Avatar 资产注册与运行时姿势模型一次性切换”，而不是继续生成图片。
